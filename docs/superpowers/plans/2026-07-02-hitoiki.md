# ひといき 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 視覚的な呼吸ガイドPWA「ひといき」を SvelteKit + adapter-static で構築する。

**Architecture:** 純TSのステートマシン(`BreathingEngine`)が `requestAnimationFrame` 由来のタイムスタンプでフェーズを進行し、Svelte 5 コンポーネントがその状態を描画する。完全静的サイトとしてビルドし、Service Worker で全アセットを事前キャッシュしてオフラインで確実に動作させる。

**Tech Stack:** Svelte 5(runes)/ SvelteKit 2 / @sveltejs/adapter-static / TypeScript / Vitest + @testing-library/svelte / Service Worker(SvelteKit `$service-worker`)

**Spec:** `docs/superpowers/specs/2026-07-02-hitoiki-design.md`

## Global Constraints

- 外部リソース(フォント・CDN・アナリティクス等)を一切読み込まない。フォントは `system-ui, sans-serif`
- 初回ロード合計 100KB 未満を目標とする
- アニメーションは `transform` と `opacity` のみ(レイアウトを発生させない)
- 色コントラストは WCAG 2.1 AA(テキスト 4.5:1、UIコンポーネント 3:1)
- すべての操作要素のタッチターゲットは 44×44px 以上
- 音声は一切使わない
- 言語は日本語のみ。フェーズ表記は「すって」「とめて」「はいて」(ひらがな)
- localStorage キーは `hitoiki:settings:v1`
- Service Worker は skipWaiting / clients.claim を使わない(利用中に強制リロードしない。新バージョンは次回起動時に有効化)
- ブラウザAPI(localStorage / Wake Lock / Vibration / Service Worker)はすべて機能検出し、非対応時は静かにフォールバックする(エラーを利用者に見せない)
- `.svelte` / `.svelte.ts` ファイルを作成・編集するタスクでは、svelte MCP の autofixer(`svelte:svelte-code-writer` スキル)でコードを検証すること

## ファイル構成(全体像)

```
src/
  app.css                              # デザイントークン(ライト/ダーク)・基礎スタイル
  app.html                             # lang="ja"、manifest リンク(Task 8)
  service-worker.ts                    # 事前キャッシュ + cache-first(Task 8)
  routes/
    +layout.ts                         # prerender = true
    +layout.svelte                     # app.css 読み込み
    +page.svelte                       # メイン画面(エンジン駆動・WakeLock・振動)
  lib/
    breathing/
      types.ts                         # PhaseName / BreathingPattern / EngineState / ラベル
      presets.ts                       # プリセット定義・clampPattern
      engine.ts                        # BreathingEngine(純TS、UIに依存しない)
    settings.svelte.ts                 # 設定の読み書き(純関数)+ runes ストア
    components/
      BreathingCircle.svelte           # 円・進捗リング・フェーズテキスト・aria-live
      ControlBar.svelte                # 開始/停止ボタン
      SettingsSheet.svelte             # <dialog> によるプリセット・スライダー・振動トグル
static/
  manifest.webmanifest                 # Task 8
  icons/icon.svg, icon-192.png, icon-512.png, apple-touch-icon.png
scripts/
  generate-icons.mjs                   # SVG から PNG アイコンを生成(sharp)
```

---

### Task 1: プロジェクト scaffold とテスト基盤

**Files:**
- Create: プロジェクト一式(`npx sv create` による scaffold)
- Create: `src/app.css`
- Modify: `src/app.html`(lang="ja")
- Modify: `svelte.config.js`(adapter-static)
- Create: `src/routes/+layout.ts`, `src/routes/+layout.svelte`
- Modify: `vite.config.ts`(Vitest 設定)
- Create: `vitest-setup.ts`

**Interfaces:**
- Consumes: なし
- Produces: `npm run dev` / `npm run build` / `npm run check` / `npm run test:run` が通る土台。CSS カスタムプロパティ(`--bg` `--fg` `--circle-bg` `--circle-fg` `--ring-track` `--ring-fg` `--button-bg` `--button-fg`)と `.visually-hidden` ユーティリティ

- [ ] **Step 1: SvelteKit プロジェクトを scaffold する**

リポジトリルート(`docs/` と `.git` がある場所)で:

```bash
npx sv create --template minimal --types ts --no-add-ons --install npm .
```

対話プロンプトが出た場合: minimal テンプレート、TypeScript、add-on なし、npm でインストール、既存ディレクトリへの作成を許可。

- [ ] **Step 2: adapter-static とテスト系の依存を追加する**

```bash
npm install -D @sveltejs/adapter-static vitest jsdom @testing-library/svelte @testing-library/jest-dom
```

- [ ] **Step 3: svelte.config.js を adapter-static に変更する**

`svelte.config.js` 全体を以下に置き換える:

```js
import adapter from '@sveltejs/adapter-static';
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

/** @type {import('@sveltejs/kit').Config} */
const config = {
	preprocess: vitePreprocess(),
	kit: {
		adapter: adapter()
	}
};

export default config;
```

- [ ] **Step 4: 全ページを prerender 対象にする**

`src/routes/+layout.ts` を作成:

```ts
export const prerender = true;
```

`src/routes/+layout.svelte` を作成:

```svelte
<script lang="ts">
	import '../app.css';

	let { children } = $props();
</script>

{@render children()}
```

- [ ] **Step 5: app.html を日本語化する**

`src/app.html` の `<html lang="en">` を `<html lang="ja">` に変更する。

- [ ] **Step 6: デザイントークンと基礎スタイルを書く**

`src/app.css` を作成:

```css
:root {
	color-scheme: light dark;
	--bg: #f6f4ef;
	--fg: #33403c;
	--fg-soft: #5a6a65;
	--circle-bg: #a7c4bc;
	--circle-fg: #223330;
	--ring-track: rgba(51, 64, 60, 0.15);
	--ring-fg: #33403c;
	--button-bg: #33403c;
	--button-fg: #f6f4ef;
}

@media (prefers-color-scheme: dark) {
	:root {
		--bg: #191d1c;
		--fg: #d9e2df;
		--fg-soft: #a5b3ae;
		--circle-bg: #3d534d;
		--circle-fg: #e2ebe8;
		--ring-track: rgba(217, 226, 223, 0.18);
		--ring-fg: #d9e2df;
		--button-bg: #d9e2df;
		--button-fg: #191d1c;
	}
}

* {
	box-sizing: border-box;
}

html,
body {
	margin: 0;
	height: 100%;
}

body {
	background: var(--bg);
	color: var(--fg);
	font-family: system-ui, sans-serif;
	-webkit-font-smoothing: antialiased;
}

button {
	font: inherit;
	color: inherit;
	background: none;
	border: none;
	cursor: pointer;
	min-width: 44px;
	min-height: 44px;
}

:focus-visible {
	outline: 2px solid var(--fg);
	outline-offset: 2px;
}

.visually-hidden {
	position: absolute;
	width: 1px;
	height: 1px;
	padding: 0;
	margin: -1px;
	overflow: hidden;
	clip-path: inset(50%);
	white-space: nowrap;
	border: 0;
}
```

- [ ] **Step 7: Vitest を設定する**

`vite.config.ts` 全体を以下に置き換える:

```ts
import { sveltekit } from '@sveltejs/kit/vite';
import { svelteTesting } from '@testing-library/svelte/vite';
import { defineConfig } from 'vite';

export default defineConfig({
	plugins: [sveltekit(), svelteTesting()],
	test: {
		environment: 'jsdom',
		setupFiles: ['./vitest-setup.ts'],
		include: ['src/**/*.test.ts']
	}
});
```

`vitest-setup.ts` を作成(jsdom に無いAPIをここで補う):

```ts
import '@testing-library/jest-dom/vitest';

// jsdom は matchMedia を実装していない(svelte/reactivity の MediaQuery が使う)
if (typeof window !== 'undefined' && !window.matchMedia) {
	window.matchMedia = (query: string) =>
		({
			matches: false,
			media: query,
			onchange: null,
			addEventListener() {},
			removeEventListener() {},
			addListener() {},
			removeListener() {},
			dispatchEvent: () => false
		}) as MediaQueryList;
}

// jsdom は requestAnimationFrame を提供しない構成がある
if (typeof globalThis.requestAnimationFrame === 'undefined') {
	globalThis.requestAnimationFrame = ((cb: FrameRequestCallback) =>
		setTimeout(() => cb(performance.now()), 16)) as unknown as typeof requestAnimationFrame;
	globalThis.cancelAnimationFrame = ((id: number) =>
		clearTimeout(id)) as unknown as typeof cancelAnimationFrame;
}

// jsdom の <dialog> は showModal 未実装のバージョンがある
if (typeof HTMLDialogElement !== 'undefined' && !HTMLDialogElement.prototype.showModal) {
	HTMLDialogElement.prototype.showModal = function () {
		this.setAttribute('open', '');
	};
	HTMLDialogElement.prototype.close = function () {
		this.removeAttribute('open');
		this.dispatchEvent(new Event('close'));
	};
}
```

`package.json` の `scripts` に追加:

```json
"test": "vitest",
"test:run": "vitest run"
```

- [ ] **Step 8: ビルドと型チェックが通ることを確認する**

Run: `npm run build && npm run check`
Expected: どちらも exit code 0(check は 0 errors)

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "chore: SvelteKit + adapter-static + Vitest の土台を構築"
```

---

### Task 2: 呼吸パターンの型とプリセット

**Files:**
- Create: `src/lib/breathing/types.ts`
- Create: `src/lib/breathing/presets.ts`
- Test: `src/lib/breathing/presets.test.ts`

**Interfaces:**
- Consumes: なし
- Produces:
  - `type PhaseName = 'inhale' | 'holdIn' | 'exhale' | 'holdOut'`
  - `interface BreathingPattern { inhale: number; holdIn: number; exhale: number; holdOut: number }`(単位: 秒)
  - `interface EngineState { running: boolean; phase: PhaseName; phaseProgress: number; remainingSeconds: number }`
  - `const PHASE_ORDER: readonly PhaseName[]`
  - `const PHASE_LABELS: Record<PhaseName, string>`
  - `type PresetId = 'gentle' | 'four78' | 'box' | 'custom'`
  - `interface Preset { id: Exclude<PresetId, 'custom'>; name: string; pattern: BreathingPattern }`
  - `const PRESETS: readonly Preset[]` / `const DEFAULT_PRESET_ID: PresetId`
  - `function clampPattern(p: BreathingPattern): BreathingPattern`(吸う・吐く 1〜10、止める 0〜10 に丸める)

- [ ] **Step 1: 失敗するテストを書く**

`src/lib/breathing/presets.test.ts` を作成:

```ts
import { describe, expect, it } from 'vitest';
import { PHASE_LABELS, PHASE_ORDER } from './types';
import { clampPattern, DEFAULT_PRESET_ID, PRESETS } from './presets';

describe('presets', () => {
	it('デフォルトプリセットは「ゆっくり」(息止めなし)', () => {
		const gentle = PRESETS.find((p) => p.id === DEFAULT_PRESET_ID);
		expect(gentle?.pattern).toEqual({ inhale: 4, holdIn: 0, exhale: 6, holdOut: 0 });
	});

	it('4-7-8呼吸とボックス呼吸を含む', () => {
		expect(PRESETS.find((p) => p.id === 'four78')?.pattern).toEqual({
			inhale: 4,
			holdIn: 7,
			exhale: 8,
			holdOut: 0
		});
		expect(PRESETS.find((p) => p.id === 'box')?.pattern).toEqual({
			inhale: 4,
			holdIn: 4,
			exhale: 4,
			holdOut: 4
		});
	});

	it('フェーズ表記はひらがな', () => {
		expect(PHASE_LABELS.inhale).toBe('すって');
		expect(PHASE_LABELS.holdIn).toBe('とめて');
		expect(PHASE_LABELS.exhale).toBe('はいて');
		expect(PHASE_LABELS.holdOut).toBe('とめて');
	});

	it('フェーズ順は 吸う→止める→吐く→止める', () => {
		expect(PHASE_ORDER).toEqual(['inhale', 'holdIn', 'exhale', 'holdOut']);
	});
});

describe('clampPattern', () => {
	it('吸う・吐くは 1〜10 秒に丸める', () => {
		const clamped = clampPattern({ inhale: 0, holdIn: 4, exhale: 99, holdOut: 4 });
		expect(clamped.inhale).toBe(1);
		expect(clamped.exhale).toBe(10);
	});

	it('止めるは 0〜10 秒に丸める', () => {
		const clamped = clampPattern({ inhale: 4, holdIn: -3, exhale: 6, holdOut: 42 });
		expect(clamped.holdIn).toBe(0);
		expect(clamped.holdOut).toBe(10);
	});

	it('範囲内の値はそのまま', () => {
		const pattern = { inhale: 4, holdIn: 0, exhale: 6, holdOut: 0 };
		expect(clampPattern(pattern)).toEqual(pattern);
	});
});
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `npm run test:run -- src/lib/breathing/presets.test.ts`
Expected: FAIL(`./types` / `./presets` が存在しないため)

- [ ] **Step 3: 実装を書く**

`src/lib/breathing/types.ts` を作成:

```ts
export type PhaseName = 'inhale' | 'holdIn' | 'exhale' | 'holdOut';

/** 各フェーズの長さ(秒) */
export interface BreathingPattern {
	inhale: number;
	holdIn: number;
	exhale: number;
	holdOut: number;
}

export interface EngineState {
	running: boolean;
	phase: PhaseName;
	/** フェーズ内の経過率 0〜1 */
	phaseProgress: number;
	/** フェーズの残り秒数(切り上げ) */
	remainingSeconds: number;
}

export const PHASE_ORDER: readonly PhaseName[] = ['inhale', 'holdIn', 'exhale', 'holdOut'];

export const PHASE_LABELS: Record<PhaseName, string> = {
	inhale: 'すって',
	holdIn: 'とめて',
	exhale: 'はいて',
	holdOut: 'とめて'
};
```

`src/lib/breathing/presets.ts` を作成:

```ts
import type { BreathingPattern } from './types';

export type PresetId = 'gentle' | 'four78' | 'box' | 'custom';

export interface Preset {
	id: Exclude<PresetId, 'custom'>;
	name: string;
	pattern: BreathingPattern;
}

export const PRESETS: readonly Preset[] = [
	{ id: 'gentle', name: 'ゆっくり', pattern: { inhale: 4, holdIn: 0, exhale: 6, holdOut: 0 } },
	{ id: 'four78', name: '4-7-8呼吸', pattern: { inhale: 4, holdIn: 7, exhale: 8, holdOut: 0 } },
	{ id: 'box', name: 'ボックス呼吸', pattern: { inhale: 4, holdIn: 4, exhale: 4, holdOut: 4 } }
];

export const DEFAULT_PRESET_ID: PresetId = 'gentle';

function clamp(value: number, min: number, max: number): number {
	return Math.min(max, Math.max(min, value));
}

/** 吸う・吐くは 1〜10 秒、止めるは 0〜10 秒に丸める */
export function clampPattern(p: BreathingPattern): BreathingPattern {
	return {
		inhale: clamp(p.inhale, 1, 10),
		holdIn: clamp(p.holdIn, 0, 10),
		exhale: clamp(p.exhale, 1, 10),
		holdOut: clamp(p.holdOut, 0, 10)
	};
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npm run test:run -- src/lib/breathing/presets.test.ts`
Expected: PASS(7 tests)

- [ ] **Step 5: Commit**

```bash
git add src/lib/breathing
git commit -m "feat: 呼吸パターンの型とプリセットを追加"
```

---

### Task 3: BreathingEngine(フェーズ進行ステートマシン)

**Files:**
- Create: `src/lib/breathing/engine.ts`
- Test: `src/lib/breathing/engine.test.ts`

**Interfaces:**
- Consumes: `BreathingPattern` / `EngineState` / `PhaseName` / `PHASE_ORDER`(Task 2)
- Produces: `class BreathingEngine`
  - `get state(): EngineState`(更新のたびに**新しいオブジェクト**を返す。Svelte の `$state` 代入で確実に再描画されるように)
  - `start(pattern: BreathingPattern, now: number): void`(now はミリ秒。`performance.now()` を渡す)
  - `tick(now: number): EngineState`(rAF から毎フレーム呼ぶ。フェーズ進行・0秒スキップ・サイクル折返しを処理)
  - `setPattern(pattern: BreathingPattern): void`(実行中は次サイクルの頭から反映)
  - `stop(): void`
  - `subscribe(fn: (s: EngineState) => void): () => void`(即時に現在値を1回通知し、以後更新のたびに通知。戻り値は解除関数)

- [ ] **Step 1: 失敗するテストを書く**

`src/lib/breathing/engine.test.ts` を作成:

```ts
import { describe, expect, it } from 'vitest';
import { BreathingEngine } from './engine';

const BOX = { inhale: 4, holdIn: 4, exhale: 4, holdOut: 4 };
const GENTLE = { inhale: 4, holdIn: 0, exhale: 6, holdOut: 0 };

describe('BreathingEngine', () => {
	it('start で最初のフェーズ(吸う)から始まる', () => {
		const e = new BreathingEngine();
		e.start(BOX, 0);
		expect(e.state.running).toBe(true);
		expect(e.state.phase).toBe('inhale');
		expect(e.state.phaseProgress).toBe(0);
		expect(e.state.remainingSeconds).toBe(4);
	});

	it('tick でフェーズ内の経過率が進む', () => {
		const e = new BreathingEngine();
		e.start(BOX, 0);
		const s = e.tick(2000);
		expect(s.phase).toBe('inhale');
		expect(s.phaseProgress).toBeCloseTo(0.5);
		expect(s.remainingSeconds).toBe(2);
	});

	it('フェーズ時間を超えると次のフェーズへ進む', () => {
		const e = new BreathingEngine();
		e.start(BOX, 0);
		const s = e.tick(4000);
		expect(s.phase).toBe('holdIn');
		expect(s.phaseProgress).toBeCloseTo(0);
	});

	it('0秒のフェーズはスキップされる', () => {
		const e = new BreathingEngine();
		e.start(GENTLE, 0);
		const s = e.tick(4000);
		expect(s.phase).toBe('exhale');
	});

	it('サイクル末尾から先頭へ戻る', () => {
		const e = new BreathingEngine();
		e.start(BOX, 0);
		const s = e.tick(16000);
		expect(s.phase).toBe('inhale');
		expect(s.phaseProgress).toBeCloseTo(0);
	});

	it('複数フェーズをまたぐ大きな時間経過も正しく処理する(タブのバックグラウンド化)', () => {
		const e = new BreathingEngine();
		e.start(BOX, 0);
		const s = e.tick(9000);
		expect(s.phase).toBe('exhale');
		expect(s.phaseProgress).toBeCloseTo(0.25);
	});

	it('実行中の setPattern は次サイクルの頭から反映される', () => {
		const e = new BreathingEngine();
		e.start(GENTLE, 0); // 1サイクル = 4 + 6 = 10 秒
		e.setPattern(BOX);
		expect(e.tick(5000).phase).toBe('exhale'); // まだ GENTLE のまま
		expect(e.tick(10000).phase).toBe('inhale'); // 折返しで BOX に切替
		expect(e.tick(14000).phase).toBe('holdIn'); // BOX にしかない holdIn に入る
	});

	it('停止中の setPattern は無視され、start に渡したパターンが使われる', () => {
		const e = new BreathingEngine();
		e.setPattern(GENTLE); // 停止中なので無視される
		e.start(BOX, 0);
		expect(e.tick(4000).phase).toBe('holdIn'); // BOX の holdIn に入る
	});

	it('stop で初期状態に戻る', () => {
		const e = new BreathingEngine();
		e.start(BOX, 0);
		e.tick(5000);
		e.stop();
		expect(e.state).toEqual({
			running: false,
			phase: 'inhale',
			phaseProgress: 0,
			remainingSeconds: 0
		});
	});

	it('全フェーズ0秒のパターンでは開始しない', () => {
		const e = new BreathingEngine();
		e.start({ inhale: 0, holdIn: 0, exhale: 0, holdOut: 0 }, 0);
		expect(e.state.running).toBe(false);
	});

	it('停止中の tick は状態を変えない', () => {
		const e = new BreathingEngine();
		const before = e.state;
		expect(e.tick(1000)).toBe(before);
	});

	it('subscribe は即時に現在値を通知し、更新のたびに呼ばれる', () => {
		const e = new BreathingEngine();
		const seen: string[] = [];
		const unsubscribe = e.subscribe((s) => seen.push(`${s.running}:${s.phase}`));
		expect(seen).toEqual(['false:inhale']);
		e.start(BOX, 0);
		e.tick(4000);
		expect(seen).toEqual(['false:inhale', 'true:inhale', 'true:holdIn']);
		unsubscribe();
		e.tick(8000);
		expect(seen.length).toBe(3);
	});
});
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `npm run test:run -- src/lib/breathing/engine.test.ts`
Expected: FAIL(`./engine` が存在しないため)

- [ ] **Step 3: 実装を書く**

`src/lib/breathing/engine.ts` を作成:

```ts
import { PHASE_ORDER, type BreathingPattern, type EngineState } from './types';

const IDLE_STATE: EngineState = {
	running: false,
	phase: 'inhale',
	phaseProgress: 0,
	remainingSeconds: 0
};

/**
 * 呼吸フェーズの進行を管理するステートマシン。UIに依存しない。
 * 時刻は外部(rAF)から渡されるため、テストでは任意のタイムスタンプで駆動できる。
 */
export class BreathingEngine {
	#pattern: BreathingPattern | null = null;
	#nextPattern: BreathingPattern | null = null;
	#phaseIndex = 0;
	#phaseStart = 0; // 現在フェーズの開始時刻(ミリ秒)
	#state: EngineState = IDLE_STATE;
	#subscribers = new Set<(s: EngineState) => void>();

	get state(): EngineState {
		return this.#state;
	}

	subscribe(fn: (s: EngineState) => void): () => void {
		this.#subscribers.add(fn);
		fn(this.#state);
		return () => this.#subscribers.delete(fn);
	}

	start(pattern: BreathingPattern, now: number): void {
		const first = PHASE_ORDER.findIndex((p) => pattern[p] > 0);
		if (first === -1) return; // 全フェーズ0秒では開始しない
		this.#pattern = { ...pattern };
		this.#nextPattern = null;
		this.#phaseIndex = first;
		this.#phaseStart = now;
		const duration = pattern[PHASE_ORDER[first]];
		this.#setState({
			running: true,
			phase: PHASE_ORDER[first],
			phaseProgress: 0,
			remainingSeconds: Math.ceil(duration)
		});
	}

	stop(): void {
		this.#pattern = null;
		this.#nextPattern = null;
		this.#setState(IDLE_STATE);
	}

	/** 実行中は次サイクルの頭から反映。停止中は次の start まで保持しない(start が pattern を受け取るため何もしない) */
	setPattern(pattern: BreathingPattern): void {
		if (this.#state.running) {
			this.#nextPattern = { ...pattern };
		}
	}

	tick(now: number): EngineState {
		if (!this.#state.running || !this.#pattern) return this.#state;

		let elapsed = (now - this.#phaseStart) / 1000;
		let duration = this.#pattern[PHASE_ORDER[this.#phaseIndex]];

		while (elapsed >= duration) {
			elapsed -= duration;
			this.#phaseStart += duration * 1000;
			if (!this.#advancePhase()) {
				this.stop();
				return this.#state;
			}
			duration = this.#pattern[PHASE_ORDER[this.#phaseIndex]];
		}

		this.#setState({
			running: true,
			phase: PHASE_ORDER[this.#phaseIndex],
			phaseProgress: duration > 0 ? elapsed / duration : 0,
			remainingSeconds: Math.ceil(duration - elapsed)
		});
		return this.#state;
	}

	/** 次の 0 秒でないフェーズへ進む。サイクル先頭へ折り返すとき保留中のパターンを適用する */
	#advancePhase(): boolean {
		let applied = false;
		// パターン切替直後に先頭フェーズが0秒のケースも拾えるよう2周分探索する
		for (let step = 1; step <= PHASE_ORDER.length * 2; step++) {
			const idx = (this.#phaseIndex + step) % PHASE_ORDER.length;
			if (idx === 0 && this.#nextPattern && !applied) {
				this.#pattern = this.#nextPattern;
				this.#nextPattern = null;
				applied = true;
			}
			if (this.#pattern![PHASE_ORDER[idx]] > 0) {
				this.#phaseIndex = idx;
				return true;
			}
		}
		return false;
	}

	#setState(next: EngineState): void {
		this.#state = next;
		for (const fn of this.#subscribers) fn(next);
	}
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npm run test:run -- src/lib/breathing/engine.test.ts`
Expected: PASS(12 tests)

- [ ] **Step 5: Commit**

```bash
git add src/lib/breathing/engine.ts src/lib/breathing/engine.test.ts
git commit -m "feat: 呼吸フェーズ進行のステートマシン BreathingEngine を追加"
```

---

### Task 4: 設定の永続化と runes ストア

**Files:**
- Create: `src/lib/settings.svelte.ts`
- Test: `src/lib/settings.svelte.test.ts`

**Interfaces:**
- Consumes: `BreathingPattern`(Task 2)、`PresetId` / `PRESETS` / `DEFAULT_PRESET_ID` / `clampPattern`(Task 2)
- Produces:
  - `const STORAGE_KEY = 'hitoiki:settings:v1'`
  - `interface SettingsData { presetId: PresetId; custom: BreathingPattern; vibration: boolean }`
  - `function loadSettings(storage: Pick<Storage, 'getItem'> | null): SettingsData`(壊れたデータ・null はデフォルトに)
  - `function saveSettings(storage: Pick<Storage, 'setItem'> | null, data: SettingsData): void`(失敗は握りつぶす)
  - `function createSettings(storage?: Storage | null)` — 戻り値(`type Settings`)は
    `presetId` / `custom` / `vibration` の getter と
    `selectPreset(id: Exclude<PresetId, 'custom'>): void`(プリセットのパターンを custom にコピー)、
    `updateCustom(patch: Partial<BreathingPattern>): void`(clamp して presetId を 'custom' に)、
    `setVibration(v: boolean): void` を持ち、各操作で自動保存する
  - **有効なパターンは常に `settings.custom`**(プリセット選択時もパターンは custom にコピーされる)

- [ ] **Step 1: 失敗するテストを書く**

`src/lib/settings.svelte.test.ts` を作成:

```ts
import { describe, expect, it } from 'vitest';
import {
	createSettings,
	loadSettings,
	saveSettings,
	STORAGE_KEY,
	type SettingsData
} from './settings.svelte';

function fakeStorage(initial: Record<string, string> = {}) {
	const map = new Map(Object.entries(initial));
	return {
		getItem: (key: string) => map.get(key) ?? null,
		setItem: (key: string, value: string) => void map.set(key, value),
		dump: () => Object.fromEntries(map)
	};
}

const DEFAULTS: SettingsData = {
	presetId: 'gentle',
	custom: { inhale: 4, holdIn: 0, exhale: 6, holdOut: 0 },
	vibration: false
};

describe('loadSettings', () => {
	it('storage が null ならデフォルトを返す', () => {
		expect(loadSettings(null)).toEqual(DEFAULTS);
	});

	it('保存が無ければデフォルトを返す', () => {
		expect(loadSettings(fakeStorage())).toEqual(DEFAULTS);
	});

	it('保存済みの設定を復元する', () => {
		const data: SettingsData = {
			presetId: 'box',
			custom: { inhale: 4, holdIn: 4, exhale: 4, holdOut: 4 },
			vibration: true
		};
		const storage = fakeStorage({ [STORAGE_KEY]: JSON.stringify(data) });
		expect(loadSettings(storage)).toEqual(data);
	});

	it('壊れた JSON はデフォルトにフォールバックする', () => {
		const storage = fakeStorage({ [STORAGE_KEY]: '{oops' });
		expect(loadSettings(storage)).toEqual(DEFAULTS);
	});

	it('範囲外の秒数は丸めて復元する', () => {
		const storage = fakeStorage({
			[STORAGE_KEY]: JSON.stringify({
				presetId: 'custom',
				custom: { inhale: 0, holdIn: 99, exhale: 6, holdOut: 0 },
				vibration: false
			})
		});
		expect(loadSettings(storage).custom).toEqual({ inhale: 1, holdIn: 10, exhale: 6, holdOut: 0 });
	});

	it('getItem が throw してもデフォルトを返す(プライベートモード)', () => {
		const storage = {
			getItem: () => {
				throw new Error('denied');
			}
		};
		expect(loadSettings(storage)).toEqual(DEFAULTS);
	});
});

describe('saveSettings', () => {
	it('JSON で保存する', () => {
		const storage = fakeStorage();
		saveSettings(storage, DEFAULTS);
		expect(JSON.parse(storage.dump()[STORAGE_KEY])).toEqual(DEFAULTS);
	});

	it('setItem が throw しても壊れない', () => {
		const storage = {
			setItem: () => {
				throw new Error('quota');
			}
		};
		expect(() => saveSettings(storage, DEFAULTS)).not.toThrow();
	});
});

describe('createSettings', () => {
	it('プリセット選択でパターンが custom にコピーされ保存される', () => {
		const storage = fakeStorage();
		const settings = createSettings(storage as unknown as Storage);
		settings.selectPreset('box');
		expect(settings.presetId).toBe('box');
		expect(settings.custom).toEqual({ inhale: 4, holdIn: 4, exhale: 4, holdOut: 4 });
		expect(JSON.parse(storage.dump()[STORAGE_KEY]).presetId).toBe('box');
	});

	it('スライダー変更で presetId が custom になり clamp される', () => {
		const settings = createSettings(fakeStorage() as unknown as Storage);
		settings.updateCustom({ inhale: 99 });
		expect(settings.presetId).toBe('custom');
		expect(settings.custom.inhale).toBe(10);
	});

	it('振動設定を保存する', () => {
		const storage = fakeStorage();
		const settings = createSettings(storage as unknown as Storage);
		settings.setVibration(true);
		expect(settings.vibration).toBe(true);
		expect(JSON.parse(storage.dump()[STORAGE_KEY]).vibration).toBe(true);
	});
});
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `npm run test:run -- src/lib/settings.svelte.test.ts`
Expected: FAIL(`./settings.svelte` が存在しないため)

- [ ] **Step 3: 実装を書く**

`src/lib/settings.svelte.ts` を作成:

```ts
import type { BreathingPattern } from './breathing/types';
import { clampPattern, DEFAULT_PRESET_ID, PRESETS, type PresetId } from './breathing/presets';

export const STORAGE_KEY = 'hitoiki:settings:v1';

export interface SettingsData {
	presetId: PresetId;
	custom: BreathingPattern;
	vibration: boolean;
}

const PRESET_IDS: readonly PresetId[] = ['gentle', 'four78', 'box', 'custom'];

function defaultData(): SettingsData {
	return {
		presetId: DEFAULT_PRESET_ID,
		custom: { ...PRESETS[0].pattern },
		vibration: false
	};
}

function isPattern(value: unknown): value is BreathingPattern {
	if (typeof value !== 'object' || value === null) return false;
	const p = value as Record<string, unknown>;
	return (['inhale', 'holdIn', 'exhale', 'holdOut'] as const).every(
		(key) => typeof p[key] === 'number' && Number.isFinite(p[key])
	);
}

export function loadSettings(storage: Pick<Storage, 'getItem'> | null): SettingsData {
	const fallback = defaultData();
	if (!storage) return fallback;
	try {
		const raw = storage.getItem(STORAGE_KEY);
		if (!raw) return fallback;
		const parsed = JSON.parse(raw) as Partial<SettingsData>;
		return {
			presetId: PRESET_IDS.includes(parsed.presetId as PresetId)
				? (parsed.presetId as PresetId)
				: fallback.presetId,
			custom: isPattern(parsed.custom) ? clampPattern(parsed.custom) : fallback.custom,
			vibration: parsed.vibration === true
		};
	} catch {
		return fallback;
	}
}

export function saveSettings(storage: Pick<Storage, 'setItem'> | null, data: SettingsData): void {
	if (!storage) return;
	try {
		storage.setItem(STORAGE_KEY, JSON.stringify(data));
	} catch {
		// プライベートモードや容量超過では保存せず動作を続ける
	}
}

function getLocalStorage(): Storage | null {
	try {
		if (typeof localStorage !== 'undefined') return localStorage;
	} catch {
		// アクセス自体が拒否される環境がある
	}
	return null;
}

export function createSettings(storage: Storage | null = getLocalStorage()) {
	const data = $state<SettingsData>(loadSettings(storage));

	function persist(): void {
		saveSettings(storage, $state.snapshot(data));
	}

	return {
		get presetId() {
			return data.presetId;
		},
		get custom() {
			return data.custom;
		},
		get vibration() {
			return data.vibration;
		},
		selectPreset(id: Exclude<PresetId, 'custom'>): void {
			const preset = PRESETS.find((p) => p.id === id);
			if (!preset) return;
			data.presetId = id;
			data.custom = { ...preset.pattern };
			persist();
		},
		updateCustom(patch: Partial<BreathingPattern>): void {
			data.custom = clampPattern({ ...data.custom, ...patch });
			data.presetId = 'custom';
			persist();
		},
		setVibration(v: boolean): void {
			data.vibration = v;
			persist();
		}
	};
}

export type Settings = ReturnType<typeof createSettings>;
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npm run test:run -- src/lib/settings.svelte.test.ts`
Expected: PASS(11 tests)

- [ ] **Step 5: Commit**

```bash
git add src/lib/settings.svelte.ts src/lib/settings.svelte.test.ts
git commit -m "feat: 設定の永続化と runes ストアを追加"
```

---

### Task 5: BreathingCircle コンポーネント

**Files:**
- Create: `src/lib/components/BreathingCircle.svelte`
- Test: `src/lib/components/BreathingCircle.test.ts`

**Interfaces:**
- Consumes: `PhaseName` / `PHASE_LABELS`(Task 2)
- Produces: `BreathingCircle.svelte` — props `{ phase: PhaseName; phaseProgress: number; running: boolean }`
  - 円のスケール: 停止時・holdOut = 0.6、inhale で 0.6→1.0(コサインイージング)、holdIn = 1.0、exhale で 1.0→0.6
  - `prefers-reduced-motion` 時はスケール振幅を 0.92〜1.0 に縮小
  - 進捗リング: フェーズ経過に応じて減っていく(色以外の時間手がかり)
  - フェーズ変化は `role="status"` + `aria-live="polite"` で通知

- [ ] **Step 1: 失敗するテストを書く**

`src/lib/components/BreathingCircle.test.ts` を作成:

```ts
import { render, screen } from '@testing-library/svelte';
import { describe, expect, it } from 'vitest';
import BreathingCircle from './BreathingCircle.svelte';

describe('BreathingCircle', () => {
	it('停止中は「ひといき」と表示する', () => {
		render(BreathingCircle, { phase: 'inhale', phaseProgress: 0, running: false });
		expect(screen.getAllByText('ひといき').length).toBeGreaterThan(0);
	});

	it('実行中はフェーズ名を表示する', () => {
		render(BreathingCircle, { phase: 'exhale', phaseProgress: 0.5, running: true });
		expect(screen.getAllByText('はいて').length).toBeGreaterThan(0);
	});

	it('実行中はフェーズ名を aria-live 領域で通知する', () => {
		render(BreathingCircle, { phase: 'inhale', phaseProgress: 0, running: true });
		expect(screen.getByRole('status')).toHaveTextContent('すって');
	});

	it('停止中は aria-live 領域を空にする', () => {
		render(BreathingCircle, { phase: 'inhale', phaseProgress: 0, running: false });
		expect(screen.getByRole('status')).toHaveTextContent('');
	});
});
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `npm run test:run -- src/lib/components/BreathingCircle.test.ts`
Expected: FAIL(コンポーネントが存在しないため)

- [ ] **Step 3: 実装を書く**

`src/lib/components/BreathingCircle.svelte` を作成:

```svelte
<script lang="ts">
	import { MediaQuery } from 'svelte/reactivity';
	import { PHASE_LABELS, type PhaseName } from '$lib/breathing/types';

	let {
		phase,
		phaseProgress,
		running
	}: { phase: PhaseName; phaseProgress: number; running: boolean } = $props();

	const reducedMotion = new MediaQuery('(prefers-reduced-motion: reduce)');

	const RING_RADIUS = 56;
	const CIRCUMFERENCE = 2 * Math.PI * RING_RADIUS;

	const restScale = $derived(reducedMotion.current ? 0.92 : 0.6);

	const scale = $derived.by(() => {
		if (!running) return restScale;
		const eased = 0.5 - 0.5 * Math.cos(Math.PI * phaseProgress);
		if (phase === 'inhale') return restScale + (1 - restScale) * eased;
		if (phase === 'exhale') return 1 - (1 - restScale) * eased;
		if (phase === 'holdIn') return 1;
		return restScale;
	});

	const label = $derived(running ? PHASE_LABELS[phase] : 'ひといき');
	const dashOffset = $derived(running ? CIRCUMFERENCE * phaseProgress : CIRCUMFERENCE);
</script>

<div class="guide">
	<svg class="ring" viewBox="0 0 120 120" aria-hidden="true">
		<circle class="ring-track" cx="60" cy="60" r={RING_RADIUS} />
		<circle
			class="ring-progress"
			cx="60"
			cy="60"
			r={RING_RADIUS}
			stroke-dasharray={CIRCUMFERENCE}
			stroke-dashoffset={dashOffset}
		/>
	</svg>
	<div class="circle" aria-hidden="true" style:transform="scale({scale})"></div>
	<p class="label" aria-hidden="true">{label}</p>
</div>
<p class="visually-hidden" role="status" aria-live="polite">{running ? label : ''}</p>

<style>
	.guide {
		position: relative;
		width: min(72vw, 320px);
		aspect-ratio: 1;
		display: grid;
		place-items: center;
	}

	.ring {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		transform: rotate(-90deg);
	}

	.ring-track,
	.ring-progress {
		fill: none;
		stroke-width: 3;
	}

	.ring-track {
		stroke: var(--ring-track);
	}

	.ring-progress {
		stroke: var(--ring-fg);
		stroke-linecap: round;
	}

	.circle {
		width: 78%;
		aspect-ratio: 1;
		border-radius: 50%;
		background: var(--circle-bg);
		will-change: transform;
	}

	.label {
		position: absolute;
		margin: 0;
		font-size: 1.75rem;
		font-weight: 600;
		letter-spacing: 0.1em;
		color: var(--circle-fg);
	}
</style>
```

- [ ] **Step 4: svelte MCP の autofixer でコンポーネントを検証する**

`svelte:svelte-code-writer` スキル(または `mcp__plugin_svelte_svelte__svelte-autofixer` ツール)に `BreathingCircle.svelte` の内容を渡し、指摘があれば修正する。

- [ ] **Step 5: テストが通ることを確認する**

Run: `npm run test:run -- src/lib/components/BreathingCircle.test.ts`
Expected: PASS(4 tests)

- [ ] **Step 6: Commit**

```bash
git add src/lib/components/BreathingCircle.svelte src/lib/components/BreathingCircle.test.ts
git commit -m "feat: 呼吸ガイド円コンポーネントを追加"
```

---

### Task 6: ControlBar とメイン画面の配線

**Files:**
- Create: `src/lib/components/ControlBar.svelte`
- Modify: `src/routes/+page.svelte`(scaffold の内容を全置換)
- Test: `src/routes/page.test.ts`

**Interfaces:**
- Consumes: `BreathingEngine`(Task 3)、`createSettings` / `Settings`(Task 4)、`BreathingCircle`(Task 5)、`PhaseName`(Task 2)
- Produces:
  - `ControlBar.svelte` — props `{ running: boolean; onstart: () => void; onstop: () => void }`。ボタン文言は「はじめる」/「とめる」
  - `+page.svelte` — rAF ループでエンジンを駆動。実行中は Wake Lock 取得、フェーズ切替時に振動(設定オン かつ 対応端末のみ)。実行中は設定ボタンを減光。`sheetOpen` state は Task 7 の SettingsSheet が使う

- [ ] **Step 1: 失敗するテストを書く**

`src/routes/page.test.ts` を作成:

```ts
import { render, screen } from '@testing-library/svelte';
import userEvent from '@testing-library/user-event';
import { describe, expect, it } from 'vitest';
import Page from './+page.svelte';

describe('メイン画面', () => {
	it('はじめるボタンで開始し、とめるボタンで停止する', async () => {
		const user = userEvent.setup();
		render(Page);

		await user.click(screen.getByRole('button', { name: 'はじめる' }));
		expect(screen.getByRole('button', { name: 'とめる' })).toBeInTheDocument();
		expect(screen.getByRole('status')).toHaveTextContent('すって');

		await user.click(screen.getByRole('button', { name: 'とめる' }));
		expect(screen.getByRole('button', { name: 'はじめる' })).toBeInTheDocument();
	});
});
```

`@testing-library/user-event` を追加:

```bash
npm install -D @testing-library/user-event
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `npm run test:run -- src/routes/page.test.ts`
Expected: FAIL(「はじめる」ボタンが見つからない)

- [ ] **Step 3: ControlBar を実装する**

`src/lib/components/ControlBar.svelte` を作成:

```svelte
<script lang="ts">
	let {
		running,
		onstart,
		onstop
	}: { running: boolean; onstart: () => void; onstop: () => void } = $props();
</script>

<button class="control" onclick={running ? onstop : onstart}>
	{running ? 'とめる' : 'はじめる'}
</button>

<style>
	.control {
		min-width: 200px;
		min-height: 56px;
		border-radius: 28px;
		background: var(--button-bg);
		color: var(--button-fg);
		font-size: 1.125rem;
		font-weight: 600;
		letter-spacing: 0.1em;
	}
</style>
```

- [ ] **Step 4: メイン画面を実装する**

`src/routes/+page.svelte` 全体を以下に置き換える:

```svelte
<script lang="ts">
	import { BreathingEngine } from '$lib/breathing/engine';
	import type { PhaseName } from '$lib/breathing/types';
	import { createSettings } from '$lib/settings.svelte';
	import BreathingCircle from '$lib/components/BreathingCircle.svelte';
	import ControlBar from '$lib/components/ControlBar.svelte';

	const settings = createSettings();
	const engine = new BreathingEngine();

	let engineState = $state(engine.state);
	let sheetOpen = $state(false);
	let rafId = 0;
	let wakeLock: WakeLockSentinel | null = null;

	function frame(now: number) {
		engineState = engine.tick(now);
		rafId = requestAnimationFrame(frame);
	}

	function startGuide() {
		engine.start({ ...settings.custom }, performance.now());
		engineState = engine.state;
		rafId = requestAnimationFrame(frame);
		void acquireWakeLock();
	}

	function stopGuide() {
		cancelAnimationFrame(rafId);
		engine.stop();
		engineState = engine.state;
		void releaseWakeLock();
	}

	async function acquireWakeLock() {
		try {
			if ('wakeLock' in navigator) wakeLock = await navigator.wakeLock.request('screen');
		} catch {
			wakeLock = null; // 非対応・拒否時はスリープ防止なしで動作する
		}
	}

	async function releaseWakeLock() {
		try {
			await wakeLock?.release();
		} catch {
			// すでに解放済みなら何もしない
		}
		wakeLock = null;
	}

	// 設定変更は次サイクルの頭から反映する
	$effect(() => {
		engine.setPattern({ ...settings.custom });
	});

	// フェーズ切替時の振動(設定オン かつ 対応端末のみ)
	let prevPhase: PhaseName = engine.state.phase;
	$effect(() =>
		engine.subscribe((s) => {
			if (s.running && s.phase !== prevPhase && settings.vibration && 'vibrate' in navigator) {
				navigator.vibrate(30);
			}
			prevPhase = s.phase;
		})
	);

	// 画面復帰時に Wake Lock を取り直す
	$effect(() => {
		const onVisibility = () => {
			if (document.visibilityState === 'visible' && engineState.running) void acquireWakeLock();
		};
		document.addEventListener('visibilitychange', onVisibility);
		return () => document.removeEventListener('visibilitychange', onVisibility);
	});

	$effect(() => () => cancelAnimationFrame(rafId));
</script>

<svelte:head>
	<title>ひといき</title>
	<meta name="description" content="呼吸に意識を向けるためのガイド" />
</svelte:head>

<main class="app" class:running={engineState.running}>
	<button class="settings-button" onclick={() => (sheetOpen = true)}>せってい</button>
	<BreathingCircle
		phase={engineState.phase}
		phaseProgress={engineState.phaseProgress}
		running={engineState.running}
	/>
	<ControlBar running={engineState.running} onstart={startGuide} onstop={stopGuide} />
</main>

<style>
	.app {
		min-height: 100dvh;
		display: grid;
		grid-template-rows: auto 1fr auto;
		justify-items: center;
		align-items: center;
		padding: 1rem 1rem calc(2rem + env(safe-area-inset-bottom));
	}

	.settings-button {
		justify-self: end;
		padding: 0.5rem 1rem;
		color: var(--fg-soft);
		transition: opacity 0.4s;
	}

	/* 実行中は視界のノイズを減らす */
	.running .settings-button {
		opacity: 0.35;
	}
</style>
```

注: `sheetOpen` はこの時点では未使用の state(svelte-check が警告しても、Task 7 で SettingsSheet に接続するため許容)。警告がエラー扱いになる場合は Task 7 完了まで `<!-- svelte-ignore -->` 等は使わず、`sheetOpen` の宣言を Task 7 に移してもよい(その場合 `settings-button` の `onclick` も Task 7 で追加)。

- [ ] **Step 5: svelte MCP の autofixer で両コンポーネントを検証する**

`ControlBar.svelte` と `+page.svelte` を autofixer にかけ、指摘があれば修正する。

- [ ] **Step 6: テストが通ることを確認する**

Run: `npm run test:run -- src/routes/page.test.ts`
Expected: PASS(1 test)

- [ ] **Step 7: ブラウザで動作確認する**

Run: `npm run dev`
確認: 「はじめる」で円が拡大縮小し、フェーズテキストが「すって」→「はいて」と切り替わる。「とめる」で初期状態に戻る。

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: メイン画面と開始/停止・WakeLock・振動を配線"
```

---

### Task 7: SettingsSheet(設定シート)

**Files:**
- Create: `src/lib/components/SettingsSheet.svelte`
- Modify: `src/routes/+page.svelte`(SettingsSheet を接続)
- Test: `src/lib/components/SettingsSheet.test.ts`

**Interfaces:**
- Consumes: `PRESETS` / `PresetId`(Task 2)、`Settings`(Task 4)
- Produces: `SettingsSheet.svelte` — props `{ open: boolean ($bindable); settings: Settings }`
  - ネイティブ `<dialog>`(showModal)でフォーカストラップと Esc 閉じを得る
  - プリセットはラジオボタン、秒数は `input[type=range]`(すう/はく: 1〜10、とめる: 0〜10)
  - 振動トグルは `'vibrate' in navigator` の端末でのみ表示

- [ ] **Step 1: 失敗するテストを書く**

`src/lib/components/SettingsSheet.test.ts` を作成:

```ts
import { fireEvent, render, screen } from '@testing-library/svelte';
import userEvent from '@testing-library/user-event';
import { describe, expect, it } from 'vitest';
import { createSettings } from '$lib/settings.svelte';
import SettingsSheet from './SettingsSheet.svelte';

describe('SettingsSheet', () => {
	it('プリセットを選ぶと設定が切り替わる', async () => {
		const user = userEvent.setup();
		const settings = createSettings(null);
		render(SettingsSheet, { open: true, settings });

		await user.click(screen.getByRole('radio', { name: 'ボックス呼吸' }));
		expect(settings.presetId).toBe('box');
		expect(settings.custom.holdIn).toBe(4);
	});

	it('スライダーを動かすとカスタム扱いになる', async () => {
		const settings = createSettings(null);
		render(SettingsSheet, { open: true, settings });

		const slider = screen.getByRole('slider', { name: /すう/ });
		await fireEvent.input(slider, { target: { value: '8' } });
		expect(settings.custom.inhale).toBe(8);
		expect(settings.presetId).toBe('custom');
	});

	it('振動非対応の環境では振動トグルを表示しない', () => {
		const settings = createSettings(null);
		render(SettingsSheet, { open: true, settings });
		// jsdom の navigator に vibrate は無い
		expect(screen.queryByRole('checkbox', { name: /振動/ })).toBeNull();
	});
});
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `npm run test:run -- src/lib/components/SettingsSheet.test.ts`
Expected: FAIL(コンポーネントが存在しないため)

- [ ] **Step 3: 実装を書く**

`src/lib/components/SettingsSheet.svelte` を作成:

```svelte
<script lang="ts">
	import { PRESETS } from '$lib/breathing/presets';
	import type { Settings } from '$lib/settings.svelte';

	let { open = $bindable(false), settings }: { open: boolean; settings: Settings } = $props();

	let dialogEl: HTMLDialogElement | undefined = $state();

	const canVibrate = typeof navigator !== 'undefined' && 'vibrate' in navigator;

	const sliders = [
		{ key: 'inhale', label: 'すう', min: 1 },
		{ key: 'holdIn', label: 'とめる(すったあと)', min: 0 },
		{ key: 'exhale', label: 'はく', min: 1 },
		{ key: 'holdOut', label: 'とめる(はいたあと)', min: 0 }
	] as const;

	$effect(() => {
		if (!dialogEl) return;
		if (open && !dialogEl.open) dialogEl.showModal();
		if (!open && dialogEl.open) dialogEl.close();
	});
</script>

<dialog bind:this={dialogEl} class="sheet" onclose={() => (open = false)}>
	<h2>せってい</h2>

	<fieldset>
		<legend>呼吸のパターン</legend>
		{#each PRESETS as preset (preset.id)}
			<label class="preset">
				<input
					type="radio"
					name="preset"
					value={preset.id}
					checked={settings.presetId === preset.id}
					onchange={() => settings.selectPreset(preset.id)}
				/>
				<span>{preset.name}</span>
			</label>
		{/each}
	</fieldset>

	<fieldset>
		<legend>秒数の調整</legend>
		{#each sliders as slider (slider.key)}
			<label class="slider">
				<span>{slider.label}: {settings.custom[slider.key]}秒</span>
				<input
					type="range"
					min={slider.min}
					max="10"
					step="1"
					value={settings.custom[slider.key]}
					oninput={(e) => settings.updateCustom({ [slider.key]: e.currentTarget.valueAsNumber })}
				/>
			</label>
		{/each}
	</fieldset>

	{#if canVibrate}
		<label class="vibration">
			<input
				type="checkbox"
				checked={settings.vibration}
				onchange={(e) => settings.setVibration(e.currentTarget.checked)}
			/>
			<span>フェーズの切り替わりで振動する</span>
		</label>
	{/if}

	<button class="close" onclick={() => (open = false)}>とじる</button>
</dialog>

<style>
	.sheet {
		width: min(100%, 420px);
		margin: auto auto 0;
		border: none;
		border-radius: 20px 20px 0 0;
		padding: 1.5rem 1.5rem calc(1.5rem + env(safe-area-inset-bottom));
		background: var(--bg);
		color: var(--fg);
	}

	.sheet::backdrop {
		background: rgba(0, 0, 0, 0.4);
	}

	h2 {
		margin: 0 0 1rem;
		font-size: 1.25rem;
	}

	fieldset {
		border: none;
		margin: 0 0 1rem;
		padding: 0;
	}

	legend {
		font-size: 0.875rem;
		color: var(--fg-soft);
		padding: 0;
		margin-bottom: 0.5rem;
	}

	.preset,
	.slider,
	.vibration {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		min-height: 44px;
	}

	.slider {
		flex-direction: column;
		align-items: stretch;
		gap: 0.25rem;
	}

	.slider input {
		width: 100%;
		min-height: 44px;
	}

	.close {
		width: 100%;
		min-height: 48px;
		border-radius: 24px;
		background: var(--button-bg);
		color: var(--button-fg);
		font-weight: 600;
	}
</style>
```

- [ ] **Step 4: メイン画面に接続する**

`src/routes/+page.svelte` の script に import を追加:

```ts
import SettingsSheet from '$lib/components/SettingsSheet.svelte';
```

`</main>` の直後に追加:

```svelte
<SettingsSheet bind:open={sheetOpen} {settings} />
```

- [ ] **Step 5: svelte MCP の autofixer で検証する**

`SettingsSheet.svelte` と変更後の `+page.svelte` を autofixer にかけ、指摘があれば修正する。

- [ ] **Step 6: テストが通ることを確認する**

Run: `npm run test:run`
Expected: 全テスト PASS(既存テストの回帰がないことも確認)

- [ ] **Step 7: ブラウザで動作確認する**

Run: `npm run dev`
確認: 「せってい」でシートが開き、プリセット切替・スライダー・とじる(および Esc)が動く。実行中にプリセットを変えると**次サイクルの頭から**反映される。リロード後も設定が残る。

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: 設定シート(プリセット・秒数調整・振動)を追加"
```

---

### Task 8: PWA(manifest・アイコン・Service Worker)

**Files:**
- Create: `static/manifest.webmanifest`
- Create: `static/icons/icon.svg`
- Create: `scripts/generate-icons.mjs`(生成物: `static/icons/icon-192.png`, `icon-512.png`, `apple-touch-icon.png`)
- Create: `src/service-worker.ts`
- Modify: `src/app.html`(manifest・theme-color・apple-touch-icon)

**Interfaces:**
- Consumes: SvelteKit の `$service-worker` モジュール(build / files / prerendered / version)
- Produces: オフラインで起動できる PWA。skipWaiting しないため新バージョンは次回起動時に有効化される

- [ ] **Step 1: アイコンSVGを作る**

`static/icons/icon.svg` を作成:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
	<rect width="512" height="512" rx="112" fill="#a7c4bc"/>
	<circle cx="256" cy="256" r="150" fill="#f6f4ef"/>
	<circle cx="256" cy="256" r="96" fill="#33403c" opacity="0.85"/>
</svg>
```

- [ ] **Step 2: PNGアイコンを生成する**

```bash
npm install -D sharp
```

`scripts/generate-icons.mjs` を作成:

```js
import { readFile } from 'node:fs/promises';
import sharp from 'sharp';

const svg = await readFile('static/icons/icon.svg');
await sharp(svg).resize(192, 192).png().toFile('static/icons/icon-192.png');
await sharp(svg).resize(512, 512).png().toFile('static/icons/icon-512.png');
await sharp(svg).resize(180, 180).png().toFile('static/icons/apple-touch-icon.png');
console.log('icons generated');
```

`package.json` の `scripts` に `"icons": "node scripts/generate-icons.mjs"` を追加して実行:

Run: `npm run icons`
Expected: `icons generated` と出力され、`static/icons/` に PNG が3つできる

- [ ] **Step 3: manifest を作る**

`static/manifest.webmanifest` を作成:

```json
{
	"name": "ひといき",
	"short_name": "ひといき",
	"description": "呼吸に意識を向けるためのガイド",
	"lang": "ja",
	"start_url": "/",
	"display": "standalone",
	"background_color": "#f6f4ef",
	"theme_color": "#f6f4ef",
	"icons": [
		{ "src": "/icons/icon.svg", "sizes": "any", "type": "image/svg+xml", "purpose": "any" },
		{ "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
		{ "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
	]
}
```

注: デプロイ先が未定のため `start_url` は `/` 前提。サブパス配信(GitHub Pages 等)に決まった場合は `paths.base` とあわせて調整する。

- [ ] **Step 4: app.html にリンクを追加する**

`src/app.html` の `<head>` 内(`%sveltekit.head%` より前)に追加:

```html
<link rel="manifest" href="%sveltekit.assets%/manifest.webmanifest" />
<meta name="theme-color" content="#f6f4ef" />
<link rel="apple-touch-icon" href="%sveltekit.assets%/icons/apple-touch-icon.png" />
```

- [ ] **Step 5: Service Worker を書く**

`src/service-worker.ts` を作成(SvelteKit が本番ビルドで自動登録する):

```ts
/// <reference types="@sveltejs/kit" />
/// <reference no-default-lib="true"/>
/// <reference lib="esnext" />
/// <reference lib="webworker" />

import { build, files, prerendered, version } from '$service-worker';

const sw = self as unknown as ServiceWorkerGlobalScope;

const CACHE = `hitoiki-${version}`;
const ASSETS = [...build, ...files, ...prerendered];

// skipWaiting / clients.claim は使わない:
// 新バージョンは全タブが閉じられた後(=次回起動時)に有効化され、利用中に画面が変わらない
sw.addEventListener('install', (event) => {
	event.waitUntil(caches.open(CACHE).then((cache) => cache.addAll(ASSETS)));
});

sw.addEventListener('activate', (event) => {
	event.waitUntil(
		caches
			.keys()
			.then((keys) => Promise.all(keys.filter((key) => key !== CACHE).map((key) => caches.delete(key))))
	);
});

// cache-first: オフラインでの確実な起動を最優先する
sw.addEventListener('fetch', (event) => {
	if (event.request.method !== 'GET') return;
	const url = new URL(event.request.url);
	if (url.origin !== sw.location.origin) return;

	event.respondWith(
		caches.match(event.request).then(
			(cached) =>
				cached ??
				fetch(event.request).then((response) => {
					if (response.ok) {
						const copy = response.clone();
						void caches.open(CACHE).then((cache) => cache.put(event.request, copy));
					}
					return response;
				})
		)
	);
});
```

- [ ] **Step 6: オフライン動作を確認する**

```bash
npm run build && npm run preview
```

ブラウザで `http://localhost:4173` を開き:
1. 一度読み込んで Service Worker が登録されることを確認(DevTools > Application > Service Workers)
2. DevTools > Network で「Offline」にしてリロード
Expected: オフラインでもアプリが表示され、呼吸ガイドが動作する

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: PWA対応(manifest・アイコン・Service Workerによるオフラインキャッシュ)"
```

---

### Task 9: README と最終検証

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: これまでの全タスク
- Produces: リポジトリの説明と、スペックの手動チェックリストの実施結果

- [ ] **Step 1: README を書く**

`README.md` を作成:

````markdown
# ひといき

呼吸に意識を向けるためのガイドです。
画面のガイドに合わせて呼吸するだけで使えます。

## 特徴

- **考えさせない**: 開いて「はじめる」を押すだけ。秒数の判断は不要
- **オフラインで動く**: PWA として全アセットをキャッシュ。地下鉄や機内でも起動できます
- **音を出さない**: 公共交通機関でも周囲を気にせず使えます
- **色に頼らない**: フェーズは 形(円の拡大縮小)・テキスト・進捗リング の複数の手がかりで伝えます

## プリセット

| プリセット | すう | とめる | はく | とめる |
|---|---|---|---|---|
| ゆっくり(デフォルト) | 4秒 | - | 6秒 | - |
| 4-7-8呼吸 | 4秒 | 7秒 | 8秒 | - |
| ボックス呼吸 | 4秒 | 4秒 | 4秒 | 4秒 |

各フェーズの秒数は設定から自由に調整できます。

## 開発

```bash
npm install
npm run dev        # 開発サーバー
npm run test       # テスト(watch)
npm run test:run   # テスト(1回)
npm run build      # 本番ビルド
npm run preview    # ビルドのプレビュー
npm run check      # 型チェック
npm run icons      # アイコンPNGの再生成
```

## 制約事項

- 本アプリは医療行為を目的とするものではなく、診断や治療を行うものではありません
- 利用者の体調変化について保証を行いません
````

- [ ] **Step 2: 全テスト・型チェック・ビルドを通す**

Run: `npm run test:run && npm run check && npm run build`
Expected: すべて exit code 0

- [ ] **Step 3: バンドルサイズを確認する**

```bash
find build -name '*.js' -exec cat {} + | gzip -c | wc -c
```

Expected: おおよそ 100000(100KB)未満。超えている場合は原因(不要な依存など)を調べて報告する。

- [ ] **Step 4: 手動チェックリストを実施する**

`npm run preview` で以下を確認し、結果を報告する:

- [ ] オフライン(DevTools > Network > Offline)でリロードして動作する
- [ ] キーボードのみで 開始 → 設定を開く → プリセット変更 → 閉じる → 停止 ができる(フォーカスリングが見える)
- [ ] OS のダークモード切替で配色が変わり、どちらのモードでも文字が読める
- [ ] OS の「視差効果を減らす」(prefers-reduced-motion)有効時、円の拡大縮小が控えめになる
- [ ] スマートフォン相当の幅(375px)で、開始/停止ボタンが親指の届く下部にある
- [ ] スクリーンリーダー(VoiceOver 等が使える場合)でフェーズ変化が読み上げられる

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: README を追加"
```
